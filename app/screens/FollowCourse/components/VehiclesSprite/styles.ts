import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  markerContainer: {
    width: 32,
    height: 32,
    overflow: 'hidden',
  },
  image: {
    width: 256,
    height: 256,
    position: 'absolute',
  },
});
